***- (WebDb/Mock) se define al usar los comandos de inicio: `pnpm dev:all` o `pnpm dev:all-mock` pero el .env requiere distinta configuración para cada caso de uso.***

### - Incluir los `.env` en `.gitignore`

# - Ejemplo `.env` Back-End para "Db" y "SignIn/SignUp" con "Mock" en servidor local:

DATA_SOURCE=MOCK
PORT=3001

# - Ejemplo `.env` para conectar con la WebDb y Auth (SignIn/SignUp) desde servidor local:

DATA_SOURCE=DB
PORT=3001

# - En MOCK local dejar comentados / descomentar para WebDb y Auth
## Configuración de Supabase (Auth & API)
#### *- Obtenidos en:* Consola Supabase -> Settings -> API
SUPABASE_URL="https://[TU_PROYECTO].supabase.co"
SUPABASE_ANON_KEY="[ANON_KEY]"

# - Solo para seeds / admin (NUNCA en Front-End ni en commits)
#### *- Obtenidos en:* Consola Supabase -> Settings -> API -> service_role
#### *- Sirve para:* `npm run seed:auth` (crear usuarios del CSV también en Auth, no solo en Postgres)
SUPABASE_SERVICE_ROLE_KEY="[SERVICE_ROLE_KEY]"

# - En MOCK local dejar comentados / descomentar para WebDb y Auth
### Cadenas de conexión para la WebDB
#### *- Obtenidos en:* Consola Supabase -> "CONNECT" *(icono "enchufe")* --> "DIRECT - CONNECTION STRING" --> "TRANSACTION_POOLER" *(para el uso de la "Planta de Tablas")*
DATABASE_URL="postgresql://postgres.[USUARIO]:[CONTRASEÑA]@aws-1-[ZONA].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# - En MOCK local dejar comentados / descomentar para WebDb y Auth
#### - Obtenidos en: Consola Supabase -> "CONNECT" (icono "ENCHUFE") --> "DIRECT - CONNECTION STRING" --> "SESSION_POOLER" *(Migración y Mantenimiento - Back-End)*
DIRECT_URL="postgresql://postgres.[USUARIO]:[CONTRASEÑA]@aws-1-[ZONA].pooler.supabase.com:5432/postgres"

# Integración con Gemini AI (Google AI Studio) (opcional)

#### - Con "CUENTA GOOGLE" --> "https://aistudio.google.com/" --> "ACCEDER" --> "PROYECTOS" *generar un proyecto nuevo, por ejemplo: "MATE+"* --> "GET API KEY" *generar y copiar la Key.*
GOOGLE_API_KEY="[TU_API_KEY]"

# - En MOCK local para tener derechos de admin el correo *tiene* que figurar acá (opcional)
### Lista de correos con permisos de administrador
ADMIN_EMAILS="[ejemplo1@ejemplo.com],[ejemplo2@ejemplo.com]"

### Evita mensajes de "actualización de Prisma" en consola (opcional)
PRISMA_HIDE_UPDATE_MESSAGE=true

**En todos los casos, para activar los ejemplos y usar la WebDb, en el `.env` local o en el panel correspondiente del host, se debe reemplazar el "[PLACEHOLDER]" completo por el valor correspondiente.**
