{
  "name": "proyecto-matematicas-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Back-End",
    "Front-End"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "lint": "npm run lint --workspace=frontend",
    "preview": "npm run preview --workspace=frontend",
    "dev:front": "npm run dev --workspace=frontend",
    "dev:back": "npm run dev --workspace=backend",
    "build": "npm --prefix Front-End run build && node -e \"const fs=require('fs'); if(fs.existsSync('Front-End/dist')) fs.cpSync('Front-End/dist', 'dist', {recursive:true});\"",
    "build:all": "npm run build:front && npm run build:back",
    "build:front": "npm --prefix Front-End run build && node -e \"const fs=require('fs'); if(fs.existsSync('Front-End/dist')) fs.cpSync('Front-End/dist', 'dist', {recursive:true});\"",
    "build:back": "npm --prefix Back-End run build",
    "install:all": "npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}